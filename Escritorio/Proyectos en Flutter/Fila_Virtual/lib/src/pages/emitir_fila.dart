import 'package:flutter/material.dart';

class EmitirFila extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.blueGrey,
      body: Container(),
    );
  }
}
